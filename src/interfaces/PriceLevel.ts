export interface ContainerProps {
    isRight: boolean;
    windowWidth: number;
  }

export interface PriceLevelRowProps {
    total: string;
    size: string;
    price: string;
    reversedFieldsOrder: boolean;
    windowWidth: number;
  }